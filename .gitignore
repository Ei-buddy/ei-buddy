# Dependencias
node_modules/

# Build / saida
dist/
build/
out/
web-build/
.next/
next-env.d.ts

# Expo
.expo/
expo-env.d.ts
.metro-health-check*

# Nativo (gerado)
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Variaveis de ambiente
.env
.env.*
!.env.example
# O modelo de producao entra no git; o .env.production de verdade, nunca —
# `.env.*` acima ja o barra, e e ele que tem segredo dentro.
!.env.production.example

# Logs
*.log
npm-debug.*
yarn-debug.*
yarn-error.*
.pnpm-debug.*

# TypeScript
*.tsbuildinfo

# Cache de ferramentas
.turbo/
.cache/
coverage/

# Sistema operacional
.DS_Store
Thumbs.db
*.tmp

# Editor (mantemos .vscode/settings.json versionado)
.idea/
*.swp
.dev

# Presets locais do Studio (UUIDs de fixture; nao versionar IDs reais) — NR-121
packages/agent/studio/presets.json
